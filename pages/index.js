import Head from 'next/head';

export default function Home() {
  return (
    <div>
      <Head>
        <title>Photo Grid</title>
        <meta name="description" content="Lazy Loading Scroll Photo Grid." />
        <link rel="icon" href="/favicon.ico" />
      </Head>
    </div>
  );
}
